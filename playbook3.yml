---
- name: Home work 3
  hosts: wordpress_server
  become: true
  become_method: ansible.builtin.sudo
  become_user: root
  roles:
    - role: monitoring
  tasks:
    - name: Display Configuration Variables
      ansible.builtin.debug:
        msg:
          - "monitoring_url: localhost:9100" # monitoring url including port
