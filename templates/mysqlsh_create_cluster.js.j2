var dbPass = "{{ mysql_root_pw }}"
var clusterName = "{{ clustername }}"

try {
  print('Setting up InnoDB cluster...\n');
  shell.connect('root@10.16.1.11:3306', dbPass)
  var cluster = dba.createCluster(clusterName);
  
  print('Adding instances to the cluster.');
  cluster.addInstance({user: 'root', host: 10.16.1.12, password: '{{ mysql_root_pw }}', port: 33060})
  print('.');
  
  cluster.addInstance({user: 'root', host: 10.16.1.13, password: '{{ mysql_root_pw }}', port: 33060})
  print('.\nInstances successfully added to the cluster.');
  print('\nInnoDB cluster deployed successfully.\n');
} catch(e) {
  print('\nThe InnoDB cluster could not be created.\n\nError: ' + e.message + '\n');
}
