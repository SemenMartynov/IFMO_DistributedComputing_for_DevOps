all:
  hosts:
    vm1:
      ansible_host: "{{ vm1_host_ip }}"
      ansible_user: "{{ vm1_ssh_user }}"
      ansible_become_password: "{{ vm1_ssh_password }}"
      ansible_ssh_private_key_file: "{{ vm1_ssh_key_path }}"
  vars:
    ansible_python_interpreter: /usr/bin/python3
