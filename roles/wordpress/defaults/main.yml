---
wordpress_image: "wordpress:latest"
wordpress_mysql_image: "mysql:5.7"
wordpress_port: 80

# Параметры WordPress DB
wordpress_db_name: "wordpress"
wordpress_db_user: "wordpress"
wordpress_db_password: "wordpress"
wordpress_db_root_password: "somewordpress"

# Репликация
wordpress_master_server_id: 1
wordpress_slave_server_id: 2
wordpress_replication_password: "repl_pass"

# Директория для docker-compose
wordpress_install_dir: "/opt/wordpress"
