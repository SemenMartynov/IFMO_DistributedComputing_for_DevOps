apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://{{ ansible_host }}:{{ prometheus_port }}
    isDefault: true
    jsonData:
      timeInterval: 15s
      httpMethod: POST