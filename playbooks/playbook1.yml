---
- name: Install docker
  hosts: blog
  become: true
  roles:
    - docker
  vars:
    docker_users: [ "{{ ansible_user }}" ]

- name: Run wordpress and db containers in docker
  hosts: blog
  become: false
  roles:
    - wordpress