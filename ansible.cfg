[defaults]
inventory = inventory
host_key_checking = False
remote_user = ansible
become = true
become_method = sudo
become_ask_pass = false

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false